import ProjectsContent from "@/Components/common/projectsContent";
import React from "react";

function Projects({ lng, ...props }) {
  return <ProjectsContent lng={lng} />;
}

export default Projects;
